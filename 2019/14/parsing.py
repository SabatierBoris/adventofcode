from cookbook import CookBook


def parse_all(datas):
    cookbook = CookBook()
    for data in datas:
        yield(parse_one_formula)
        print(
        needed, product=data.split("=>")

        print(needed, product)
    return cookbook

def parse_one_formula(formula)

